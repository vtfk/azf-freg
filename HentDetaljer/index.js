module.exports = async function (context, req) {
    // Hent en access token

    // Kjør en spørring mot freg

    // Repack resultatet til noe kult

    // Returner til klient

    return { status: 200, body: 'Noe kult' }
}